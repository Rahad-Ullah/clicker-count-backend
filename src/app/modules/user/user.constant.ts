export enum USER_ROLES {
  SUPER_ADMIN = 'super admin',
  ADMIN = 'admin',
  USER = 'user',
  ADVERTISER = 'advertiser',
}

export enum USER_STATUS {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
}

export enum GENDER {
  MALE = 'male',
  FEMALE = 'female',
  OTHER = 'other',
}

export enum USER_PRIVACY {
  PUBLIC = 'public',
  FRIENDS = 'friends',
  ONLY_ME = 'only me',
}
