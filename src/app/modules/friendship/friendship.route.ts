import express from 'express';
import { FriendshipController } from './friendship.controller';

const router = express.Router();

// router.get('/', FriendshipController);

export const friendshipRoutes = router;